import React from "react";
import "./header.scss";

const Header = () => (
  <div className="header">
    <h2>
      This is a <span className="text-yellow">Header</span> section
    </h2>
  </div>
);

export default Header;